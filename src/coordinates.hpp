#pragma once

#include "common.hpp"

/*
This might be unecessary we will see
*/

/*
class Body {
public:
    double X() { return point_.X(); }
    double Y() { return point_.Y(); }
    double Z() { return point_.Z(); }

private:
    common::Vec3 point_;
};

class Ned {
public:
    double N() { return point_.X(); }
    double E() { return point_.Y(); }
    double D() { return point_.Z(); }

private:
    common::Vec3 point_;
};

class Enu {
public:
    double E() { return point_.X(); }
    double N() { return point_.Y(); }
    double U() { return point_.Z(); }

private:
    common::Vec3 point_;

};
*/